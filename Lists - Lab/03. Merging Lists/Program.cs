using System;
using System.Collections.Generic;
using System.Linq;

namespace _03._Merging_Lists
{
    internal class Program
    {
        static void Main(string[] args)
        {
























            //List <int> firstNumbers = Console.ReadLine().Split().Select(int.Parse).ToList();
            //List <int> seconfNumbers = Console.ReadLine().Split().Select(int.Parse).ToList();
            //List<int> result = new List<int>();
            

            //int bigest = Math.Max(firstNumbers.Count, seconfNumbers.Count);
            //for (int i = 0; i < bigest; i++)
            //{
            //    if (firstNumbers.Count > i)
            //    {
            //       result.Add(firstNumbers[i]);
            //    }
            //    if (seconfNumbers.Count > i )
            //    {
            //        result.Add(seconfNumbers[i]);
            //    }
            //}
            //Console.WriteLine(string.Join(" ",result));



            //List<int> firstList = Console.ReadLine().Split().Select(int.Parse).ToList();
            //List<int> secondList = Console.ReadLine().Split().Select(int.Parse).ToList();
            //List<int> result = new List<int>();

            //int bigest = Math.Max(firstList.Count, secondList.Count);

            //for (int i = 0; i < bigest; i++)
            //{
            //    if (firstList.Count > i) 
            //    {
            //        result.Add(firstList[i]);
            //    }
            //    if (secondList.Count > i)
            //    {
            //        result.Add(secondList[i]);
            //    }

            //}
            //Console.WriteLine(String.Join(" ", result));
        }
    }
}
