using System;
using System.Collections.Generic;
using System.Linq;

namespace _04._SoftUni_Parking
{
    internal class Program
    {
        static void Main(string[] args)
        {






















            //---------------------------------------------

            //Dictionary<string, string> registered = new Dictionary<string, string>();

            //int numOfCmd = int.Parse(Console.ReadLine());

            //string input = "";

            //for (int i = 0; i < numOfCmd; i++)
            //{
            //    input = Console.ReadLine();
            //    string[] tokens = input.Split();

            //    string regOrNo = tokens[0];
            //    string username = tokens[1];


            //    if (regOrNo == "register")
            //    {
            //        string licensePlateNumber = tokens[2];

            //        if (!registered.ContainsKey(username))
            //        {
            //            registered.Add(username, licensePlateNumber);
            //            Console.WriteLine($"{username} registered {licensePlateNumber} successfully");
            //        }
            //        else
            //        {
            //            Console.WriteLine($"ERROR: already registered with plate number {licensePlateNumber}");
            //        }
            //    }
            //    else if (regOrNo == "unregister")
            //    {
            //        if (!registered.ContainsKey(username))
            //        {
            //            Console.WriteLine($"ERROR: user {username} not found");
            //        }
            //        else
            //        {
            //            registered.Remove(username);
            //            Console.WriteLine($"{username} unregistered successfully");
            //        }
            //    }


                
            //}


            //foreach (var user in registered)
            //{
            //    Console.WriteLine($"{user.Key} => {user.Value}");
            //}


            //------------------------------------------------------

            //Dictionary<string, string> parkingUsers
            //     = new Dictionary<string, string>();

            // int numOfCommands = int.Parse(Console.ReadLine());
            // string input ;

            // for (int i = 0; i < numOfCommands; i++)
            // {
            //     input = Console.ReadLine();
            //     string[] tokens = input.Split();

            //     string registerOrUnregister = tokens[0];
            //     string username = tokens[1];


            //     if (registerOrUnregister == "register")
            //     {
            //         string numOfCar = tokens[2];
            //         if (!parkingUsers.ContainsKey(username))
            //         {
            //             parkingUsers[username] = numOfCar;
            //             Console.WriteLine($"{username} registered {numOfCar} successfully");

            //         }
            //         else
            //         {
            //             string registeredPlateNumber = parkingUsers[username];
            //             Console.WriteLine($"ERROR: already registered with plate number {numOfCar}");
            //         }
            //     }
            //     else if(registerOrUnregister == "unregister")
            //     {
            //         if (!parkingUsers.ContainsKey(username))
            //         {
            //             Console.WriteLine($"ERROR: user {username} not found");
            //         }
            //         else
            //         {
            //             parkingUsers.Remove(username);
            //             Console.WriteLine($"{username} unregistered successfully");
            //         }
            //     }



            // }
            // foreach (var user in parkingUsers)
            // {
            //     Console.WriteLine($"{user.Key} => {user.Value}");
            // }


        }
    }
}
