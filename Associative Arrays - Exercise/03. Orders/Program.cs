using System;
using System.Collections.Generic;
using System.Linq;

namespace _03._Orders
{
    internal class Program
    {
        static void Main(string[] args)
        {






















            //-----------------------------------------------
            //Dictionary<string, List<double>> orders = new Dictionary<string, List<double>>();

            //string input = Console.ReadLine();

            //while (input != "buy")
            //{
            //    string[] tokens = input.Split();

            //    string product = tokens[0];
            //    double price = double.Parse(tokens[1]);
            //    int qty = int.Parse(tokens[2]);

            //    if (!orders.ContainsKey(product))
            //    {
            //        orders.Add(product, new List<double>(){price, qty });
            //    }
            //    else
            //    {
            //        orders[tokens[0]][0] = price;
            //        orders[tokens[0]][1] += qty;
            //    }
                

            //    input = Console.ReadLine();
            //}


            //foreach (var item in orders)
            //{
            //    Console.WriteLine($"{item.Key} -> {item.Value[0] * item.Value[1]:f2}");
            //}

            //-------------------------------------------

            //Dictionary<string,List<double>> products = new Dictionary<string, List<double>>();

            //string input = Console.ReadLine();

            //while (input != "buy")
            //{
            //    string[] tokens = input.Split();
            //    double price = double.Parse(tokens[1]);
            //    int quantity = int.Parse(tokens[2]);

            //    if (!products.ContainsKey(tokens[0]))
            //    {
            //        products.Add(tokens[0], new List<double>() { price,quantity});
            //    }
            //    else
            //    {
            //        products[tokens[0]][0] = price;
            //        products[tokens[0]][1] += quantity;
            //    }

               
            //    input = Console.ReadLine();
            //}

            //foreach (var prod in products)
            //{
            //    Console.WriteLine($"{prod.Key} -> {prod.Value[0] * prod.Value[1]:f2}");
            //}

        }
    }
}
