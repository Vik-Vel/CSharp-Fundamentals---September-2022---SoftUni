using System;
using System.Collections.Generic;
using System.Linq;

namespace _02._A_Miner_Task
{
    internal class Program
    {
        static void Main(string[] args)
        {





























            //-------------------------------------------------------

            //Dictionary<string, int> quantAll = new Dictionary<string, int>();

            //string input = Console.ReadLine();

            //while (input != "stop")
            //{
            //    int quantity = int.Parse(Console.ReadLine());

            //    if (!quantAll.ContainsKey(input))
            //    {
            //        quantAll.Add(input, 0);
            //    }

            //    quantAll[input] += quantity;

            //    input = Console.ReadLine();
            //}

            //foreach (var res in quantAll)
            //{
            //    Console.WriteLine($"{res.Key} -> {res.Value}");
            //}


            //-------------------------------------------

        //   string input;

        //    Dictionary<string, int> resourses = new Dictionary<string, int>();

        //    while ((input = Console.ReadLine()) != "stop") 
        //    {
        //       int inputValue = int.Parse(Console.ReadLine());
        //        if (!resourses.ContainsKey(input))
        //        {
        //            resourses[input] = 0;
        //        }
        //        resourses[input] += inputValue;

        //    }

        //    foreach (var rqp in resourses)
        //    {
        //        Console.WriteLine($"{rqp.Key} -> {rqp.Value}");
        //    }

        }
    }
}
