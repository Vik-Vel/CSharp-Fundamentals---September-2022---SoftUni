using System;
using System.Collections.Generic;
using System.Text.RegularExpressions;

namespace _02._Fancy_Barcodes
{
    internal class Program
    {
        static void Main(string[] args)
        {























            //-------------------------------

        //    int n = int.Parse(Console.ReadLine());
        //    string patternBarcode = @"(@#+)(?<barcode>[A-Z][A-Za-z0-9]{4,}[A-Z])\1";
        //    string patternDigits = @"\d+";


        //    Regex regexBarc = new Regex(patternBarcode);
        //    Regex regexDigits = new Regex(patternDigits);


        //    for (int i = 0; i < n; i++)
        //    {
        //        string input = Console.ReadLine();

        //        Match match = regexBarc.Match(input);
        //        if (match.Success)
        //        {
        //            MatchCollection matchesDigits = regexDigits.Matches(match.Value);


        //            if (matchesDigits.Count > 0)
        //            {
        //                Console.WriteLine($"Product group: {string.Join("", matchesDigits)}");
        //            }
        //            else
        //            {
        //                Console.WriteLine($"Product group: 00");
        //            }
        //        }
        //        else
        //        {
        //            Console.WriteLine("Invalid barcode");
        //        }



        //    }


            //--------------------------------------------------

            //int n = int.Parse(Console.ReadLine());
            //string pattern = @"@#+[A-Z][A-Za-z0-9]{4,}[A-Z]@#+";

            //Regex barcodeRegex = new Regex(pattern);
            //Regex productGroupRegex = new Regex(@"\d+");

            //for (int i = 0; i < n; i++)
            //{
            //    string input = Console.ReadLine();

            //    Match match = barcodeRegex.Match(input);

            //    if (match.Success)
            //    {
            //        MatchCollection productGroupMatch = productGroupRegex.Matches(match.Value);

            //        if (productGroupMatch.Count > 0)
            //        {
            //            Console.WriteLine($"Product group: {string.Join("", productGroupMatch)}");
            //        }
            //        else
            //        {
            //            Console.WriteLine($"Product group: 00");
            //        }
            //    }

            //    else
            //    {
            //        Console.WriteLine("Invalid barcode");
            //    }


            //}










        }
    }
}
