using System;
using System.Text;

namespace _01._World_Tour
{
    internal class Program
    {
        static void Main(string[] args)
        {



























            //---------------------------------------

            //string destination = Console.ReadLine();

            //string input = Console.ReadLine();

            //while (input != "Travel")
            //{
            //    string[] tokens = input.Split(':',StringSplitOptions.RemoveEmptyEntries);

            //    string command = tokens[0];

            //    if (command == "Add Stop")
            //    {
            //        int index = int.Parse(tokens[1]);
            //        string str = tokens[2];

            //        if (index >= 0 && index <= destination.Length)
            //        {
            //            destination = destination.Insert(index, str);
                        
            //        }
            //        Console.WriteLine(destination);
            //    }
            //    else if (command == "Remove Stop")
            //    {
            //        int startIndex = int.Parse(tokens[1]);
            //        int endIndex = int.Parse(tokens[2]);

            //        if (startIndex >= 0 && endIndex < destination.Length)
            //        {
            //            destination = destination.Remove(startIndex, endIndex-startIndex+1);
                        
            //        }
            //        Console.WriteLine(destination);
            //    }
            //    else if (command == "Switch")
            //    {
            //        string oldStr = tokens[1];
            //        string newStr = tokens[2];

            //        destination = destination.Replace(oldStr, newStr);
            //        Console.WriteLine(destination);
            //    }

            //    input = Console.ReadLine();
            //}

            //Console.WriteLine($"Ready for world tour! Planned stops: {destination}");




            //--------------------------------------

            //string stops = Console.ReadLine();

            //StringBuilder sb = new StringBuilder(stops);


            //while (stops != "Travel")
            //{
            //    string[] tokens = stops.Split(":",StringSplitOptions.RemoveEmptyEntries);

            //    string comand = tokens[0];

            //    if (comand == "Add Stop")
            //    {
            //        int index = int.Parse(tokens[1]);
            //        string str = tokens[2];
            //        if (index>=0 && index <= sb.Length)
            //        {
            //            sb.Insert(index, str);
            //        }


            //        Console.WriteLine(sb);
            //    }
            //    else if (comand == "Remove Stop")
            //    {
            //        int start = int.Parse(tokens[1]);
            //        int end = int.Parse(tokens[2]);

            //        int lenght = end - start+1;
            //        if (start >= 0 && end < sb.Length)
            //        {
            //            sb.Remove(start, lenght);

            //        }
            //        Console.WriteLine(sb);

            //    }
            //    else if (comand == "Switch")
            //    {
            //        string old = tokens[1];
            //        string newS = tokens[2];

            //        sb.Replace(old, newS);

            //        Console.WriteLine(sb);
            //    }


            //    stops = Console.ReadLine();
            //}

            //Console.WriteLine($"Ready for world tour! Planned stops: {sb}");


        }
    }
}
