using System;
using System.Collections.Generic;
using System.Text.RegularExpressions;

namespace _02._Destination_Mapper
{
    internal class Program
    {
        static void Main(string[] args)
        {






















            //string pattern = @"(=|\/)(?<destination>[A-Z][A-Za-z]{2,})\1";

            //Regex regex = new Regex(pattern);

            //string input = Console.ReadLine();

            //MatchCollection matches = regex.Matches(input);

            //List<string> destination = new List<string>();

            //foreach (Match match in matches)
            //{
            //    string place = match.Groups["destination"].Value;

            //    destination.Add(place);
            //}

            //int travelPoints = 0;

            //foreach (var place in destination)
            //{
            //    travelPoints += place.Length;

                
            //}
            //Console.WriteLine($"Destinations: {string.Join(", ", destination)}");
            //Console.WriteLine($"Travel Points: {travelPoints}");
            



            //-----------------------------------

            //string input = Console.ReadLine();
            //string pattern = @"([=|/])(?<destination>[A-Z][A-Za-z]{2,})\1";

            //Regex regex = new Regex(pattern, RegexOptions.Compiled);

            //MatchCollection matches = regex.Matches(input);

            //List<string> destinations = new List<string>();
            //foreach (Match item in matches)
            //{
            //    string destination = item.Groups["destination"].Value;

            //    destinations.Add(destination);
            //}

            //int points = 0;

            //foreach (var word in destinations)
            //{
            //    points += word.Length;

            //}

            //Console.WriteLine($"Destinations: {string.Join(", ",destinations)}");
            //Console.WriteLine($"Travel Points: {points}");





            //string pattern = @"(=|\/)(?<destination>[A-Z][A-Za-z]{2,})\1";
            ////@"([=/])([A-Z][A-Za-z]{2,1})\1"
            ////@"(=|\/)(?<location>[A-Z][A-z]{2,})\1"

            //int treavelPoints = 0;
            //string input = Console.ReadLine();

            //Regex regex = new Regex(pattern,RegexOptions.Compiled);




            //List<string> output = new List<string>();

            //foreach (Match match in regex.Matches(input))
            //{
            //    string dest = match.Groups["destination"].Value;
            //    output.Add(dest);
            //    treavelPoints += dest.Length;
            //}



            //Console.WriteLine($"Destinations: {string.Join(", ", output)}");
            //Console.WriteLine($"Travel Points: {treavelPoints}");


        }
    }
}
