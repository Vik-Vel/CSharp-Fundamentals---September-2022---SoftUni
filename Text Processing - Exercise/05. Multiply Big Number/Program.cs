using System;
using System.Text;

namespace _05._Multiply_Big_Number
{
    internal class Program
    {
        static void Main(string[] args)
        {























            //-------------------------------------

            //var sb = new StringBuilder();

            //string num = Console.ReadLine();
            //int multiplier = int.Parse(Console.ReadLine());

            //int reminder = 0;
            //if (num == "0" || multiplier == 0)
            //{
            //    Console.WriteLine(0);
            //    return;
            //}

            //for (int i = num.Length - 1; i >= 0; i--)
            //{
            //    int currDigit = int.Parse(num[i].ToString());
            //    int product = currDigit * multiplier + reminder;
            //    int result = product % 10;
            //    reminder = product / 10;
            //    sb.Insert(0, result);

            //}


            //if (reminder > 0)
            //{
            //    sb.Insert(0, reminder);
            //}

            //Console.WriteLine(sb.ToString());


            //-------------------------------------------------

            //string input =Console.ReadLine();
            //  int multiplier = int.Parse(Console.ReadLine());

            //  var sb = new StringBuilder();
            //  int reminder = 0;
            //  if (multiplier == 0 || input == "0")
            //  {
            //      Console.WriteLine(0);
            //      return;
            //  }
            //  // za namirane na ogromno chislo, razdelq se i se raboti s vsqko chislo po otdelno

            //  for (int i = input.Length - 1; i >= 0; i--)
            //  {
            //      int currDigit = int.Parse(input[i].ToString());
            //      int product = currDigit * multiplier + reminder;
            //      int result = product % 10;
            //      reminder = product / 10;
            //      sb.Insert(0, result);

            //  }
            //      if (reminder > 0)
            //      {
            //          sb.Insert(0, reminder);
            //      }

            //      Console.WriteLine(sb.ToString());



        }
    }
}
